{% extends 'base.html.twig' %}

{% block body %}
    <h1>Panelroom</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ panelRoom.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ panelRoom.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('panelroom_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('panelroom_edit', { 'id': panelRoom.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
