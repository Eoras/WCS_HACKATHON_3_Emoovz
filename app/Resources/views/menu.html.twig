<div class="jumbotron">
    <h1 class="panel-title text-center">CONNECTÉ <a href="{{ path('disconnect') }}" class="pull-right">X</a>
    </h1>
</div>
<div class="menu">
    <ul>
        {% for menuRoom in menuRooms %}
            <li class="thumbnail thumbnail2">
                {{ menuRoom.name }}
            </li>
        {% endfor %}
    </ul>
</div>