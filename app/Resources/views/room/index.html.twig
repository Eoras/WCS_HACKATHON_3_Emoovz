{% extends 'base.html.twig' %}

{% block body %}

    <div class="jumbotron">
        <h1 class="panel-title text-center">Choisissez vos pièces</h1>
    </div>
    <div class="row">
        <div class="col-sm-3">

        </div>
        <div class="col-sm-9">
            <div class="row mainZone">
            {% for room in rooms %}
                <div class="col-sm-3">
                    <a href="#" class="thumbnail">{{ room.name }}</a>
                </div>
            {% endfor %}
            </div>
        </div>
    </div>
    <ul>
        <li>
            <a href="{{ path('room_new') }}">Autres</a>
        </li>
    </ul>
{% endblock %}
