{% extends 'base.html.twig' %}

{% block body %}
    <h1>Moveout</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ moveOut.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ moveOut.email }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('moveout_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('moveout_edit', { 'id': moveOut.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
